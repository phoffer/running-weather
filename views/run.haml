%table.runs
  - @r.attributes.each do |att,val|
    %tr
      %td= att
      %td= val
  - unless @r.condition.pws.empty?
    - @r.condition.attributes.each do |att,val|
      %tr
        %td= att
        %td= val


%a{href: '/' } home

%a{href: '/logout' } logout
